<!DOCTYPE html>
<!-- The outermost framework for a webpage. inside of which you import the other, smaller partials. this is the template used by all pages unless specified otherwise. -->
<html>
	<head>
		<title>Shenandoah</title>
		<meta name="viewport" content="width=device-width, initial-scale=1, user-scalable=0" />
		<%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
		<%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
		<%= csrf_meta_tags %>
	</head>
	<body>
	<!-- render the nav bar, whatever page you're at, and then the footer -->
		<%= render 'layouts/nav' %>
		<%= yield %>
		<%= render 'layouts/footer' %>
	</body>
</html>
